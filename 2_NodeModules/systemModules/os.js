/* To use a system module in your current module you need to use require(<moduleName>)*/
const os = require('os');

/* Print the contents of the os module */
console.log(os);

/* Print the name of the operating system */
console.log(os.type());
